<UserControl x:Class="OpenApiConnectedService.Package.Views.ServiceEndpointWizardPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenApiConnectedService.Package.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Margin="0 0 0 10">
            <TextBlock>Enter a service URL to Swagger.json and select Go.</TextBlock> 
        </StackPanel>
        <StackPanel Margin="0 10">
            <Label Content="_URI:" Target="{Binding ElementName=ServiceUriTextBox}" />
            <TextBox  Name="ServiceUriTextBox" Text="{Binding Path=ServiceUri}"></TextBox>
        </StackPanel>
        <StackPanel Margin="0 10">
            <Label Content="_Name:" Target="{Binding ElementName=ServiceNameTextBox}" />
            <TextBox  Name="ServiceNameTextBox" Text="{Binding Path=ServiceName}"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="70">_Go</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
